#include "mge/eventSystem/Observer.hpp"
#include <SFML/Window/Event.hpp>
#include <vector>
#include <iostream>
#pragma once
class Subject
{
	
public:
	Subject(); 
	~Subject(); 
	/*
	void addHandler(Observer& pObserver);
	

	void removeHandler(Observer& pObserver);
	*/
	
private:
	// std::vector<Observer>* _observers;
protected: 

	// void notify(sf::Event pEvent);
	
};